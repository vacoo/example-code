export interface Options {
    support_phone_drive: string;
    vodopad_drive_version: string;
}

export const initialOptions: Options = {
    support_phone_drive: '',
    vodopad_drive_version: '',
};
